Frontend changes:
1) Re setup Supabase
    1) Link new api keys
    2) Tables Creation
        - accountsTable (account_id, created_at, project_ids)
        - datasetsTable (dataset_id, created_at, dataset_json)
        - parametersTable (parameter_id, created_at, name, description)
        - experimentsTable (experiment_id, created_at, dataset_id, parameter_ids, result, name)
        - projectsTable (project_id, created_at, dataset_ids, parameter_ids, experiment_ids, api_key, labrat_json, project_name)
    3) OAuth Setup - Google API
    4) 





-- Quick experiment -> Information about synthesis
-- default parameters
-- cost calc
-- sythesize dataset, leading to where
-- json content
report page
DASHBOARD PAGE 


API LINKING
